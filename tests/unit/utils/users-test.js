import users from 'github-explorer/utils/users';
import { module, test } from 'qunit';

module('Unit | Utility | users');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = users();
  assert.ok(result);
});
